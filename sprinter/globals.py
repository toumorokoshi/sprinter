import yaml


def get_global(path):
    global_config_path = os.path.join(path, ".global")
    yaml.load(


class Globals(object):

    def __init__(self, global_config):
        self._path = path

    def
